<h2>Lista de usuarios:</h2>
<hr>
<a href="/usuarios/registro"><button class="btn btn-success">Novo usuário</button></a>

{{#each usuarios}}

<div class="card mt-4">
  <div class="card-body">
    <h4>{{nome}}</h4>
    <input type="hidden" name="id" value="{{_id}}">
    <a href="/admin/usuarios/edit/{{_id}}"><button class="btn btn-secondary mt-3">Editar usuário</button></a>
    <a href="/admin/usuarios/deletar/{{_id}}"><button class="btn btn-danger mt-3">Deletar usuário</button></a>
  </div>
</div>

{{else}}
<h3 class="mt-3">Nenhum usuário registrada!</h3>

{{/each}}