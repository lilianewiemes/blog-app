<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao Blog do Node!</h1>
  <p class="lead">Este é um blog simples, desenvolvido em Node.JS, Express e MongoDB</p>
  <hr class="my-4">
  <p>Aqui você pode ler sobre as novidades no mundo da computação</p>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<hr>

<h2>Postagens recentes:</h2>

{{#each postagens}}

<div class="card">
  <div class="card-body">
    <h3>{{titulo}}</h3>
    <p>{{conteudo}}</p>
    <a href="/postagens/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
    <hr>
    <small>Categoria: {{categoria.nome}}</small>
    <small>Data da publicação: {{data}}</small>
  </div>
</div>

{{else}}
<h5>Nenhuma postagem cadastrada</h5>
{{/each}}